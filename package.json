{
    "name": "wispr",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite --host --port 7001",
        "build": "vite build",
        "preview": "vite preview",
        "prettier": "prettier --write **/*.js **/*.json **/*.html"
    },
    "devDependencies": {
        "prettier": "2.7.1",
        "vite": "^3.0.0"
    }
}
